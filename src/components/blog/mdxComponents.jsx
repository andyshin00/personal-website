import Note from "./Note";

// Every component listed here can be used directly inside any .mdx post,
// e.g. <Note>...</Note>, with no import needed in the post file itself.
// To add a new one: make a component (like Note.jsx) and list it below.
export const mdxComponents = {
  Note,
};
